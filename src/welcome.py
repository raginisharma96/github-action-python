def greet(name:str) -> str:
    """This function takes a name and returns that
     name along with the string concatenated.

    Args:
        name (str): name

    Returns:
        str: concatenated string with name.
    """

    returnString = "Welcome to the year 2020, "+name
    return returnString